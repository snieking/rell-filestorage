query get_file(hash: byte_array, pubkey): gtv {
	val gtx_tx = gtx_transaction.from_bytes(stored_file@{ 
		.hash == hash, 
		.pubkey == pubkey
	} ( .transaction.tx_data ));
	return gtx_tx.operations[0].args[0];	
}