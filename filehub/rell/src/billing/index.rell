import ft3_acc: lib.ft3.account;
import ft3_core: lib.ft3.core;

entity voucher {
	key account: ft3_acc.account, timestamp;
	plan;
	ft3_core.asset;
	amount: integer;
}

enum plan {
	CHROMIA,
	COMMON
}