{
  "name": "test",
  "version": "1.0.0",
  "private": true,
  "description": "Test project for the file storage dapp",
  "scripts": {
    "test-ci": "jest --setupFiles dotenv/config --group=ci",
    "silent-test-ci": "jest --setupFiles dotenv/config --silent --group=ci",
    "test-common-ci": "jest --setupFiles dotenv/config --group=common-ci",
    "silent-test-common-ci": "jest --setupFiles dotenv/config --silent --group=common-ci",
    "test-migration": "jest --setupFiles dotenv/config --group=migration",
    "silent-test-migration": "jest --setupFiles dotenv/config --silent --group=migration",
    "test-common-migration": "jest --setupFiles dotenv/config --group=common-migration",
    "silent-test-common-migration": "jest --setupFiles dotenv/config --silent --group=common-migration"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^13.1.2",
    "@types/secp256k1": "3.5.0",
    "dotenv": "^8.2.0",
    "fs-client": "file:../client",
    "ft3-lib": "^2.1.0",
    "jest-runner-groups": "^1.0.0",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "@types/jest": "^24.0.25",
    "@types/scrypt-async": "^1.3.0",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0"
  }
}
